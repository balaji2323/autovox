services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DJANGO_DEBUG: "False"
      DJANGO_SECRET_KEY: "change-this-before-production"
      DJANGO_ALLOWED_HOSTS: "localhost,127.0.0.1"
      EMAIL_HOST: "smtp.gmail.com"
      EMAIL_PORT: "587"
      EMAIL_USE_TLS: "True"
      EMAIL_HOST_USER: ""
      EMAIL_HOST_PASSWORD: ""
      DEFAULT_FROM_EMAIL: ""
      AUTOVOX_ENQUIRY_EMAIL: "k.balaji2312@gmail.com"
